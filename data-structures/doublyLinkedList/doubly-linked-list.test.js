const { Node, DoublyLinkedList, flatten } = require('./doubly-linked-list');

describe('Doubly Linked List', () => {
  test('Flattens Multilevel Doubly Linked List', () => {
    const testList = new DoublyLinkedList();
    testList.head = new Node(1);
  });
});