import {Node, LinkedList, emmm} from '../emmm';

describe('Eenie Meeney Miney Moe', () => {
  test('', () => {
    let list = new LinkedList();
    list.head = new Node(1);
    list.insert(2);
    list.insert(3);
    list.insert(4);
    list.insert(5);
    list.insert(6);

  });
});